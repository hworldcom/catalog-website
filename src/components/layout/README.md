# Layout Components

Shared application layout components live here.

Use this folder for shells, navigation, footers, empty states, and error
surfaces that are reused across domains. Domain-specific layout should stay in
the owning feature.
