# Admin Components

Feature-local admin UI components live here.

Shared layout and product UI belongs outside this folder.

`ClassifierImportShell` provides the contained authenticated layout shared by
the classifier-import inbox and detail screens.
