# Account Feature

Owns future buyer or user account surfaces, such as profile, preferences, and
account settings.

Current status: scaffold only.

No `/account` routes or visible account screens exist yet. Add them only after
buyer/user account requirements are defined.

Boundaries:

- Keep seller dashboard workflows in `src/features/seller`.
- Keep admin workflows in `src/features/admin`.
- Keep authentication entry points in `src/features/auth`.
